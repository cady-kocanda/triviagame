This is the React + Vite client for the Trivia Game.

To run in development:

    cd client
    npm install
    npm run dev

To build for production and serve via the backend:

    cd client
    npm install
    npm run build
    # copy /client/dist contents to /public or the server will serve /client/dist when present

